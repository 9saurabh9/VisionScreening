export type Theme = {
    colors: {
      background: string;
      // Add more colors as needed
    };
  };
  
export const theme: Theme = {
    colors: {
      background: 'rgb(255, 205, 189, 0.25)',
    },
  };